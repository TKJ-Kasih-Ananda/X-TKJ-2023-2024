#include <iostream>
int main() {
 int total;  // Inisialisasi total integer
 std::cout << "masukan total: ";
 std::cin >> total;  // total input
  // Using for loop
 for (int i = 0; i < total; ++i) {
 std::cout << std::endl;  // Output
    }
    return 0;
}